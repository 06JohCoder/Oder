const myShowModal = document.getElementById("myShowModal")
function openModal() {
    myShowModal.classList.add('is-active');
  }

function closeModal() {
    myShowModal.classList.remove('is-active');
  }